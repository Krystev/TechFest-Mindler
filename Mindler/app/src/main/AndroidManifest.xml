<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.inveitix.mindler">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.MainActivity" />
        <activity android:name=".ui.AddQuestionsActivity" />
        <activity android:name=".ui.StartTestActivity" />
        <activity android:name=".ui.EnterTestActivity" />
        <activity android:name=".ui.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.AnswerQuestionActivity"></activity>
        <activity android:name=".ui.ChooseQuestionsActivity" />
        <activity android:name=".ui.ChooseTestActivity" />
        <activity android:name=".ui.FinalResultActivity"></activity>
    </application>

</manifest>